{
  "name": "weema-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @weema/admin dev",
    "dev:admin": "pnpm --filter @weema/admin dev",
    "dev:facilitator": "pnpm --filter @weema/facilitator dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  },
  "pnpm": {
    "overrides": {
      "flatted": "^3.4.2",
      "picomatch": "^4.0.4"
    }
  }
}
